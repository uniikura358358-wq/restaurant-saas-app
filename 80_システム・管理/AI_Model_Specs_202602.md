# AI Model Technical Boundaries & Parameter Ranges
**Usage:** Reference for safe tuning. Not a strict rulebook.
**Purpose:** To define the "Safe Zone" where API errors do not occur.

## 1. Model Identity (Hard Limits)
以下のID以外を指定すると `404 Not Found` エラーとなり動作しません。

| Model Name | **System ID (Must Match)** | Note |
| :--- | :--- | :--- |
| **Main** | **`gemini-3-flash-preview`** | プレビュー版のためID変更の可能性あり。 |
| **Sub** | **`gemini-2.5-flash`** | 安定板。 |
| **Image** | `imagen-3.0-generate-001` | 画像生成用。 |

---

## 2. Parameter Tuning Ranges (Adjustable)
エラーにならずに調整可能な範囲（Safe Zone）。

| Parameter | **Safe Range (Recommended)** | **Hard Limit (Crash/Error)** | Tuning Effect |
| :--- | :--- | :--- | :--- |
| **Temperature** | `0.0` - `2.0` | `< 0.0` or `> 2.0` | **高**: 創造的(0.7~)。**低**: 正確(0.2~)。 |
| **Max Tokens** | `100` - `8192` (Flash) | `> 1,048,576` (Context Limit) | **増**: 長文回答。**減**: 高速化・コスト削減。 |
| **Timeout (Client)** | `5s` - `60s` | `Browser Limit` (often ~120s) | **短**: 待たせない(UX)。**長**: 思考時間を確保。 |
| **Images (Qty)** | `1` - `4` | `> 4` (API Quota Limit) | **多**: バリエーション増。**少**: 高速化。 |

> **💡 Note**: `gemini-3-flash` は思考プロセスがあるため、Timeout は `30s` 以上確保すると安全です。それ以下に詰める場合は `2.5-flash` を推奨します。

---

## 3. Error Boundaries (Trigger Conditions)
「これをやるとエラーになる」というシステム的な境界線。

* **400 Bad Request**:
    * **原因**: プロンプトが空 (`""`)、または安全フィルター（Safety Settings）に抵触。
    * **対策**: 入力チェックを行う。`BLOCK_NONE` は API キー権限によっては設定不可。
* **429 Too Many Requests**:
    * **境界線**:
        * Free Tier: `15 RPM` (1分間に15回) を超えた瞬間。
        * Pay-as-you-go: `1,000 RPM` を超えた瞬間。
    * **調整**: ループ処理のウェイト（`sleep`）で調整可能。
* **Context Window**:
    * **境界線**: 入力テキストが `1,000,000 tokens` (約70万文字) を超えるとエラー。
    * **調整**: 超える場合は、古いログを切り捨てる処理が必要。

---

## 4. Logical Fallback Strategy (Suggestion)
エラー発生時にシステムを止めないための推奨フロー（強制ではありません）。

1.  **Main Model (3 Flash)** で試行。
    * *Timeout設定*: 自由に調整 (推奨 30s)
2.  **エラー検知 (4xx/5xx)**
    * ユーザーに見せるか、裏でリトライするかはUX次第。
3.  **Sub Model (2.5 Flash)** へ切り替え。
    * *Timeout設定*: 短めでOK (推奨 10s)

---

## 5. Physical Constraints (Storage)
* **File Path**: `/public/images/`
    * ※Next.jsの仕様上、ここに置かないとブラウザから即座にアクセスできません。